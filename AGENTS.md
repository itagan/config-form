# Repository Development Workflow

对本仓库执行功能开发或缺陷修复时，默认遵循以下流程：

1. 修改前检查 `git status`、`git diff` 和当前分支，保留用户已有变更。
2. 从本地 `master` 创建独立任务分支：功能使用 `codex/feature-<name>`，修复使用 `codex/fix-<name>`。
3. 实现保持任务范围内的最小改动，不混入无关重构。
4. 完成后运行相关测试，并在合并前运行 `pnpm release:check`。
5. 将变更拆成清晰提交，工作区干净后切回 `master`，使用 `git merge --no-ff` 合并。
6. 默认保留本地任务分支，不删除；除非用户要求，否则不推送远端。
7. 如存在来源不明的修改、无法安全切换分支或合并冲突，停止并向用户说明，不擅自覆盖。

只读分析、代码审查和问题诊断不触发分支、提交或合并。
